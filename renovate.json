{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "packageRules": [
    {
      "description": "Merge anything short of a major version once the checks pass. A refreshed API description arrives as a digest update, and codegen regenerates the node definitions on the branch before the checks run.",
      "matchUpdateTypes": ["minor", "patch", "pin", "digest"],
      "automerge": true,
      "automergeType": "pr"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Keep the pinned GitHub OpenAPI description current; run go generate ./... after it updates",
      "managerFilePatterns": ["/^gen/openapi\\.ref$/"],
      "matchStrings": ["(?<currentDigest>[a-f0-9]{40})"],
      "depNameTemplate": "https://github.com/github/rest-api-description",
      "currentValueTemplate": "main",
      "datasourceTemplate": "git-refs"
    }
  ]
}
